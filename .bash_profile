# .bash_profile — QNAP QTS (bash 3.2)
# Sourced by login shells. Sources .bashrc for interactive config.
# Survives in /share/NFSv=4/homes/admin/ across QTS firmware updates.

# Source .bashrc if it exists
[ -f "$HOME/.bashrc" ] && . "$HOME/.bashrc"
